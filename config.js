
const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "rootcitas",
      password: "MySQLDataLean",
      database: "citasbd",
    },
    listPerPage: 10,
  };
  module.exports = config;